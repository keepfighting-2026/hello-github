<!DOCTYPE html>
<html>
<body>
<h3>账号登录</h3>
<form>
账号：<input placeholder="输入账号">
密码：<input type="password" placeholder="输入密码">
确认密码：<input type="password" placeholder="重复密码">
<button>登录</button>
<button>切换注册</button>
</form>

<script>
const btn = document.getElementsByTagName('button')[0]
let reg = false
btn.onclick = ()=>{
  const user = document.querySelectorAll('input')[0].value
  const pwd = document.querySelectorAll('input')[1].value
  const repwd = document.querySelectorAll('input')[2].value

  if(user == '') alert('账号不能为空')
  fetch("/login",{
    method:"POST",
    body:{user,pwd}
  }).then(res=>{
    alert("登录成功")
  })
}
</script>
</body>
</html>
这篇文档只是用于记录一下我第一次写html语言代码。还有豆包参与修改。。。。。。总之我想表达的意思就是大概率还是会有很多错误
所以如果有人看到，不要闲的没事搬一下，这只是一坨回忆而已。
